# Clerk (https://dashboard.clerk.com) — Configure > API Keys
# Publishable key — safe to expose to the browser, but must keep the VITE_
# prefix or Vite will not include it in the client bundle.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
